# Не редактировать папку base/

Папка `base/` — замороженный снимок документации на старт разработки Godot.

Агент и человек **не изменяют** файлы внутри `base/`.
Рабочие документы и код — только в корне проекта и в `scenes/`, `scripts/` и т.д.
